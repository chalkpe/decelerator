{
  "name": "@decelerator/database",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "default": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "prisma generate && tsc --build --incremental",
    "dev": "prisma studio",
    "start": "prisma db push --skip-generate && prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^6.12.0",
    "@tsconfig/node24": "^24.0.1"
  },
  "devDependencies": {
    "prisma": "^6.12.0"
  }
}
